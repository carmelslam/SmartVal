<!DOCTYPE html>
<html lang="he" dir="rtl">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>ירון כיוף שמאות - התחלת דוח</title>
  <link rel="icon" href="https://carmelcayouf.com/wp-content/uploads/2025/04/logo-yaron.webp" type="image/webp">
  <link href="https://fonts.googleapis.com/css2?family=Assistant&display=swap" rel="stylesheet" />
  <style>
    body {
      font-family: sans-serif;
      margin: 0;
      display: flex;
      justify-content: center;
      align-items: center;
      min-height: 100vh;
      background: #f3f6fb;
      padding: 30px;
    }
    .container {
      max-width: 480px;
      margin: auto;
      background: #fff;
      padding: 40px 30px;
      border-radius: 16px;
      text-align: center;
      box-shadow: 0 0 20px rgba(0,0,0,0.1);
    }
    .logo {
      text-align: center;
      margin-bottom: 20px;
    }
    .logo img {
      width: 120px;
      opacity: 0.8;
    }
    .title {
      text-align: center;
      font-size: 24px;
      font-weight: bold;
      margin-bottom: 5px;
    }
    .subtitle {
      text-align: center;
      font-size: 20px;
      color: #666;
     font-weight: semi-bold;
      margin-bottom: 21px;
     
    }
     h1 {
     font-size: 24px;
      margin: 10px 0;
     }
    h2 {
      text-align: center;
      margin-bottom: 22px;
      font-size: 20px;
      color: #007bff;
    }
    input, button {
      width: 100%;
      padding: 12px;
      margin: 10px 0;
      border-radius: 10px;
      font-size: 16px;
      border: 1px solid #ccc;
      box-sizing: border-box;
    }
    button {
      background-color: #007bff;
      color: white;
      border: none;
      font-weight: bold;
     font-family: sans-serif;
      cursor: pointer;
    }
    .footer {
      text-align: center;
      margin-top: 40px;
      font-size: 12px;
      color: #999;
    }
    .error {
      color: red;
      text-align: center;
      font-size: 14px;
      margin-bottom: 10px;
    }
  </style>
</head>
<body>
  <div class="container">
    <div class="logo">
      <img src="https://carmelcayouf.com/wp-content/uploads/2025/04/logo-yaron.webp" alt="Logo" />
    </div>
    <div class="title">ירון כיוף שמאות - פורטל</div>
    <div class="subtitle">שמאי רכב והערכת נזקי רכוש</div>
    <h2>התחל דו"ח חדש</h2>
    <div id="err" class="error"></div>
   <input id="plate" type="text" placeholder="מספר רכב" />
    <input id="owner" type="text" placeholder="שם בעל הרכב" />
    <input id="date" type="date" />
    <input id="location" type="text" placeholder="מקום בדיקה" />
    <button id="submitBtn">שלח דו"ח</button>

    <div class="footer">© All rights reserved @ Carmel Cayouf</div>
  </div>

  <script>
  document.getElementById("date").value = new Date().toISOString().split("T")[0];

  const password = sessionStorage.getItem("ycPass");
  if (!password) {
    alert("גישה חסומה. התחבר קודם דרך הדף הראשי.");
    window.location.href = "index.html";
  }

  document.getElementById("submitBtn").addEventListener("click", async function () {
    const plate = document.getElementById("plate").value.trim();
    const owner = document.getElementById("owner").value.trim();
    const date = document.getElementById("date").value.trim();
    const location = document.getElementById("location").value.trim();

    if (!plate || !owner || !date || !location) {
      document.getElementById("err").innerText = "נא למלא את כל השדות";
      return;
    }

    const data = { plate, owner, date, location, password };

    // Save to sessionStorage
    sessionStorage.setItem("plate", plate);
    sessionStorage.setItem("owner", owner);
    sessionStorage.setItem("date", date);
    sessionStorage.setItem("location", location);

   try {
  const res = await fetch("https://hook.eu2.make.com/zhvqbvx2yp69rikm6euv0r2du8l6sh61", {
    method: "POST",
    headers: { "Content-Type": "application/json" },
    body: JSON.stringify(data)
  });

  const contentType = res.headers.get("Content-Type");
  const raw = await res.text();

  if (!res.ok) {
    document.getElementById("err").innerText = "שגיאה מהשרת: " + raw;
    return;
  }

  if (contentType && contentType.includes("application/json")) {
    // If JSON → parse it
    try {
      const parsed = JSON.parse(raw);
      const deep = typeof parsed[0]?.value === "string" ? JSON.parse(parsed[0].value) : parsed[0]?.value;
      sessionStorage.setItem("carData", JSON.stringify(deep));
      alert("🚗 הדו״ח נשלח בהצלחה ✅");
    } catch (e) {
      document.getElementById("err").innerText = "בעיה בפענוח תגובת JSON מהשרת.";
      console.error("Parse failed:", e, raw);
    }
  } else {
    // Plain text response (like "Accepted")
    sessionStorage.setItem("carData", JSON.stringify({ result: raw }));
    alert("✅ הדו\"ח התקבל: " + raw);
  }
} catch (err) {
  document.getElementById("err").innerText = "שגיאה כללית בשליחה: " + err.message;
}

  });
</script>
<script src="car-details-float.js"></script>


</body>
</html>
